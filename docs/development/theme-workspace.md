# 工作区与阅读层主题（THEME-03）

更新：2026-09-23。状态：**THEME-03 代码与自动验证已完成，浏览器视觉验收由用户进行。** 对应[浅色主题计划](../planning/light-theme-todo.md)第三项；协作任务 [Issue #33](https://github.com/wolfhawkld/living-memory/issues/33)。

本项接入主工作区、搜索/领域/待同步、账号管理、学习历史与练习、长期保持及 Markdown 大窗。3D/2D 画布主题与 Mermaid 图表本身仍由 THEME-04/05 处理。

## 本轮进度

- [x] 创建独立分支 `feat/theme-workspace`，基于 THEME-02。
- [x] 新建 `src/web/theme-workspace.ts`，定义浅色组件语义变量和六种状态色。
- [x] 主工作区、Markdown 大窗/正文/公式错误色接入；节点列表、图例与曲线使用语义状态变量。
- [x] 账号/导航、学习弹窗接入及统一复核。
- [x] 根主题变量与图谱过渡范围接入，移除 THEME-02 的工作区深色限制。
- [x] 相关 Node 检查、完整回归和构建：255/255 通过，构建通过。
- [x] 更新使用说明与任务状态。

若用户中途要求暂停，停止后续操作，保留工作树及进度；恢复时先查看 Git 状态再从未完成项继续。浏览器视觉验证由用户进行。

## 已保存的实现

- `theme-workspace.ts` 提供浅色组件语义变量与六种状态色。CSS 使用原深色值作为回退，保留现有深色效果。
- 工作区、账号管理、搜索、领域与待同步、学习历史、场景练习、信心与长期保持、Markdown 阅读大窗及正文已接入；页面状态颜色改为 CSS 变量，因此切换主题无需重建图谱或回答表单。
- 根主题变量现在覆盖完整页面，移除 THEME-02 的工作区深色限制；`.graph-frame` 单独保留深色变量与原生控件配色，图谱场景及其覆盖层留到 THEME-04。
- 表格、代码、引用和公式错误色随主题变化；普通图片和 Mermaid 图表本身仍按 THEME-05 待办，不改变用户媒体内容。

## 验证

- 2026-09-23 重新运行 `npm test`：255/255 通过，包括 2 项新增浅色文字对比度及页面/图谱颜色作用域测试。
- 同日重新运行 `npm run build`：通过，保留既有图谱大包体积提示；本地 `dist` 已更新。
- 只读对比本轮 10 个组件样式文件，解析颜色变量的深色回退后，与分支起点的声明一致；布局、尺寸、滚动及深色原值差异为 0。
- `git diff --check`：通过。未运行浏览器视觉测试。

## 协作与后续

工作分支：`feat/theme-workspace`，基于 THEME-02 的 `feat/theme-preferences`（[PR #32](https://github.com/wolfhawkld/living-memory/pull/32)），通过草稿 PR 交付，不自动合并。

2026-09-22 完成代码后按用户要求暂停；2026-09-23 按“继续执行”恢复验证与协作提交。下一项为 THEME-04 图谱主题适配，之后是 THEME-05 Mermaid/媒体；整套浅色视觉的人工验收保留在 THEME-06。
